function Hero() {
    return (
        <div>
            <p className="hero">This is a real app with Node.js backend - use <strong> "admin@flatlogic.com / password" </strong>to login!</p>
        </div>
    )
};

export default Hero;